buildscript {
    dependencies {
        classpath libs.aspectjtools
        classpath libs.aspectjweaver
        // ... 其他 classpath 依赖
    }
}
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.android.library) apply false
}
apply from: "appVersion.gradle"
